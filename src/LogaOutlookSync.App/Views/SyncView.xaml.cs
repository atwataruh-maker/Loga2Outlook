using System.Windows.Controls;

namespace LogaOutlookSync.App.Views;

/// <summary>Codebehind für <see cref="SyncView"/>. Enthält bewusst keine Logik (reine Datenbindung).</summary>
public partial class SyncView : UserControl
{
    public SyncView()
    {
        InitializeComponent();
    }
}

using System.Windows.Controls;

namespace LogaOutlookSync.App.Views;

/// <summary>Codebehind für <see cref="LogView"/>. Enthält bewusst keine Logik (reine Datenbindung).</summary>
public partial class LogView : UserControl
{
    public LogView()
    {
        InitializeComponent();
    }
}

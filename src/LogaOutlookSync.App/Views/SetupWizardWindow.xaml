<Window x:Class="LogaOutlookSync.App.Views.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:LogaOutlookSync.App.Views"
        Title="LOGA Outlook Sync - Einrichtung"
        Height="760" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:SettingsView x:Name="SettingsHost" Grid.Row="0" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Abbrechen" Style="{StaticResource SecondaryButton}" Click="OnCancelClicked" />
            <Button Content="Fertig" Click="OnFinishClicked" />
        </StackPanel>
    </Grid>
</Window>
